import VerifyCertificateClient from "./VerifyCertificateClient";

/** التحقق من الشهادة لأي `code` في المسار — عرض ديناميكي (SSR). */
export default function VerifyCertificatePage() {
    return <VerifyCertificateClient />;
}
