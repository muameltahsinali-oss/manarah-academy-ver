import { SettingsClient } from "@/app/(app)/dashboard/settings/SettingsClient";

export const metadata = {
    title: "إعدادات المدرب - منارة اكاديمي",
};

export default function InstructorSettingsPage() {
    return <SettingsClient />;
}
