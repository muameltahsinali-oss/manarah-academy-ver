import { CreateCourseClient } from "./CreateCourseClient";

export const metadata = {
    title: "إنشاء دورة - منارة اكاديمي",
};

export default function CreateCoursePage() {
    return <CreateCourseClient />;
}
