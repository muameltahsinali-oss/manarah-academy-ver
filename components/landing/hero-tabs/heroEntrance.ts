/** Guest hero: مدة ظهور الـ loader كحالة أولية قبل الكشف عن المحتوى. */
export const GUEST_HERO_ENTRANCE_MS = 720;
export const GUEST_HERO_ENTRANCE_REDUCED_MS = 380;

/** انتقالات النافذة (ثوانٍ) — إجمالي الإحساس ~600–1200ms مع التدرّج. */
export const HERO_MORPH_MS = 0.48;
export const HERO_CONTENT_REVEAL_MS = 0.52;
export const HERO_TAB_STAGGER_S = 0.055;
export const HERO_TAB_STAGGER_DELAY_S = 0.12;
